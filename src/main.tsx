import React from 'react';
import ReactDOM from 'react-dom/client';
import ReviewMeals from './pages/planner/ReviewMeals';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ReviewMeals />
  </React.StrictMode>
);
